import React, { Component } from 'react'

export default class ExemploState extends Component {
  state = {
    user: {
      name: 'Matheus',
      email: 'matheus.calegaro@b2wdigital.com',
      isCool: true
    }
  }

  render() {
    const { user } = this.state

    return (
      <ul>
        <li>{user.name}</li>
        <li>{user.email}</li>
        <li>{user.isCool ? 'You\'re cool :)' : 'Not cool :('}</li>
      </ul>
    )
  }
}
